<section class="text-center">
  <nav class='text-center'>
    <%#= binding.pry %>
    <% if current_backend_admin.email %>
      <%= link_to('Logout', destroy_backend_admin_session_path, method: :delete, class: 'btn btn-danger') %>
    <% else %>
      <%= link_to('Login', new_backend_admin_session_path)  %>
    <% end %>
  </nav>
  <h1>Market Watcher</h1>
  <%= link_to "Markets List", backend_markets_path, class: 'btn btn-info', data: { turbolinks: false } %>
  <%= link_to "Opportunities List", backend_opportunities_path, class: 'btn btn-info', data: { turbolinks: false } %>
  <%= link_to "Exchanges List", backend_exchanges_path, class: 'btn btn-info', data: { turbolinks: false } %>
  <%= link_to "Sidekiq", '/backend/sidekiq', class: 'btn btn-info', data: { turbolinks: false } %>
</section>
